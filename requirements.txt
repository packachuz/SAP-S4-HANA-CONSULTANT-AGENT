# Core RAG / NotebookLM
# Pinned to the 0.5.x line: this is an unofficial client whose API can drift
# between minor releases, and src/notebooklm_client.py is verified against 0.5.0.
notebooklm-py[browser]>=0.5.0,<0.6.0
playwright>=1.44.0

# LLM orchestration
google-genai>=0.3.0
# Optional — durable RAG state on Cloud Run (notebook registry, podcasts)
google-cloud-storage>=2.16.0
# openai>=1.30.0   # uncomment if you prefer OpenAI for orchestration

# UI
streamlit>=1.36.0
streamlit-extras>=0.4.0

# Async + utility
aiofiles>=23.2.1
httpx>=0.27.0
python-dotenv>=1.0.1
pydantic>=2.7.0
loguru>=0.7.2
tenacity>=8.3.0
rich>=13.7.0

# Audio playback / podcast handling
pydub>=0.25.1
